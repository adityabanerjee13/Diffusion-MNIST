run the training code for ddpm model with - "python -m model_access_tools.train"
run the sampleing code for ddpm model with -"python -m model_access_tools.sampler"

Dataset must be inserted in /data/train.

The score model is not yet Working.